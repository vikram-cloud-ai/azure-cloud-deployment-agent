AZURE_OPENAI_API_VERSION="YOUR_API_VERSION_HERE"
AZURE_OPENAI_ENDPOINT = "MiCROSOFT FOUNDRY PROJECT ENDPOINT HERE"
AZURE_DEPLOYMENT_NAME = "FOUNDRY MODEL DEPLOYMENT NAME HERE"
AZURE_OPENAI_KEY='YOUR_API_KEY_HERE'

# Slack approval (optional)
# Option A – Bot API (full approval via thread reply: set both vars)
# Requires a Slack app with chat:write and channels:history scopes
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_CHANNEL_ID=C0123456789

# Option B – Incoming Webhook (one-way notification only; interrupt still used for approval)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx